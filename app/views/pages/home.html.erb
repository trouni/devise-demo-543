<div class="container py-5">
  <h1>Doug 😎</h1>
  <p>Find me in app/views/pages/home.html.erb</p>
  <% if user_signed_in? %>
    <p>Signed is as:</p>
    <%= current_user.email %>
  <% end %>
</div>
